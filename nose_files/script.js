const sea = lottie.loadAnimation({
    container: document.getElementById('sea'), // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'https://static.igem.wiki/teams/5993/animations/seawave.json' // the path to the animation json
  });